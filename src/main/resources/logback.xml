<configuration>
    <jmxConfigurator/>

    <!--    <appender name="metrics" class="io.prometheus.client.logback.InstrumentedAppender" />-->

    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] [%X{node_id}] %highlight(%-5level) %cyan(%logger{15}) - %msg %n
            </pattern>
        </encoder>
    </appender>

    <appender name="apiConsole" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %highlight(%-5level) %cyan(%logger{15}) - %msg %n</pattern>
        </encoder>
    </appender>


    <logger name="org.tessellation" level="debug">
        <appender-ref ref="console"/>
    </logger>

    <logger name="org.http4s" level="info">
        <appender-ref ref="apiConsole"/>
    </logger>

    <root level="info">
        <appender-ref ref="metrics"/>
    </root>
</configuration>
