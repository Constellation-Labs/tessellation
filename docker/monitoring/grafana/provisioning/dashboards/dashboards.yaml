apiVersion: 1
providers:
  - name: tessellation
    orgId: 1
    folder: Tessellation
    type: file
    allowUiUpdates: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: false
