type: object
description: Information about the next DAG price and when it will take effect
properties:
  price:
    type: integer
    format: int64
    minimum: 0
    description: The next DAG price in datum/USD format
  asOfEpoch:
    $ref: './EpochProgress.yml'
required:
  - price
  - asOfEpoch
